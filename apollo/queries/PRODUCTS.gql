query PRODUCTS($per_page: Int!, $page: Int!, $brand: String) {
  products(per_page: $per_page, page: $page, brand: $brand) {
    nodes {
      id
      slug
      title
      price
      images {
        index
        url
      }
      gender
      brands {
        id
        title
        slug
      }
    }
    meta {
      count
      lastPage
      currentPage
      total
    }
  }
}
